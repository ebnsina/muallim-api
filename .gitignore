# Internal planning + scratch data — never committed. The rest of docs/ ships.
docs/plan.md
data/

# Secrets
.env
.env.*
!.env.example
*.pem
*.key

# Build output — `make build` writes bin/, a bare `go build` drops the binary here
/bin/
/tmp/
/api
/migrate
/seed
/worker
*.exe

# Go
*.test
*.out
coverage.*
vendor/

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
